<%= form_with(model: guide_code) do |form| %>
  <% if guide_code.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(guide_code.errors.count, "error") %> prohibited this guide_code from being saved:</h2>

      <ul>
        <% guide_code.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
